# This file was automatically generated.

[package]
edition = "2021"
name = "jctest"
version = "0.1.0"

[dependencies]
cortex-m = { version = "0.7.7", features = ["inline-asm", "critical-section-single-core"] }
cortex-m-rt = "0.7.5"
defmt = { version = "0.3.8", optional = true }
defmt-rtt = { version = "0.4.1", optional = true }
embassy-executor = { version = "0.6.3", features = ["task-arena-size-32768", "arch-cortex-m", "executor-thread", "defmt", "integrated-timers"] }
embassy-futures = "0.1.1"
embassy-stm32 = { version = "0.1.0", features = ["defmt", "memory-x", "stm32l4s5qi", "time-driver-any", "exti", "chrono", "unstable-pac"] }
embassy-sync = "0.6.0"
embassy-time = { version = "0.3.2", features = ["defmt", "defmt-timestamp-uptime","tick-hz-32_768"] }
embedded-io = "0.6.1"
heapless = { version = "0.8", default-features = false }
panic-probe = { version = "0.3.2", features = ["print-defmt"], optional = true }
cortex-m-semihosting = { version = "0.5.0", optional = true }

[[bin]]
name = "jctest"
test = false
bench = false

[profile.dev]
debug = true
lto = true
opt-level = "z"
incremental = true

[profile.release]
debug = false
lto = true
opt-level = "z"
incremental = true

[features]
defmt = ["dep:defmt"]
defmt-rtt = ["dep:defmt-rtt"]
panic-probe = ["dep:panic-probe"]
default = ["debug"]
debug = [
    "defmt",
    "defmt-rtt",
    "panic-probe",
    "embassy-executor/defmt",
    "embassy-sync/defmt",
    "embassy-futures/defmt",
    "embassy-time/defmt",
    "embassy-time/defmt-timestamp-uptime",
    "embassy-stm32/defmt",
]
